version: '3.7'
services:
    database:
        image: 'mysql:5.7'
        environment:
            MYSQL_ROOT_PASSWORD: password
            MYSQL_DATABASE: paragin_assignment
        ports:
            - '3306'

